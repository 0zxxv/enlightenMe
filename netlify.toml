[build]
  command = "npm ci && npx expo export --platform web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback so Expo Router client routes work on refresh / deep links
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
