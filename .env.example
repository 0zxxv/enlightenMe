# Local / phone (PC LAN or hotspot IP)
EXPO_PUBLIC_API_URL=http://localhost:3001/api/v1

# Production (Netlify + Render) — set this in Netlify env vars:
# EXPO_PUBLIC_API_URL=https://YOUR-RENDER-SERVICE.onrender.com/api/v1
